{
  "name": "your-project",
  "version": "1.0.0",
  "description": "Project running client, server and Ollama",
  "dependencies": {
    "concurrently": "^9.2.1"
  },
  "scripts": {
    "start": "concurrently \"npm run dev --prefix client\" \"npm run dev --prefix server\" \"ollama run llama3.2\"",
    "install:all": "npm install && npm install --prefix client && npm install --prefix server"
  },
  "devDependencies": {},
  "keywords": [],
  "author": "",
  "license": "ISC"
}